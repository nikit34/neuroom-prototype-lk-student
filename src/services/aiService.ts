import { Alert } from 'react-native';

const aiFeedbackVariants: string[] = [
  'Работа выполнена на хорошем уровне. Обратите внимание на оформление — есть небольшие замечания по структуре ответа.',
  'Отличная работа! Все задания решены верно. Рекомендую также изучить дополнительные материалы по теме.',
  'Есть ошибки в 2-3 заданиях. Пересмотрите формулы и попробуйте решить задачи заново.',
  'Работа хорошая, но не хватает подробных пояснений к решениям. Добавьте промежуточные шаги.',
  'Видно, что вы разобрались в теме. Однако в последнем задании допущена вычислительная ошибка.',
  'Задание выполнено частично. Не хватает ответов на вопросы 4 и 5. Дополните работу.',
  'Превосходно! Чётко, аккуратно и без ошибок. Так держать!',
  'Работа принята. Обратите внимание: в задаче 3 использован устаревший метод решения. Попробуйте новый подход из параграфа 8.',
  'Хорошая попытка, но есть существенные пробелы в понимании темы. Рекомендую повторить теоретический материал.',
  'Задание выполнено в срок и на достойном уровне. Мелкие недочёты в оформлении не влияют на оценку.',
];

export async function analyzeHomework(homeworkId: string): Promise<string> {
  return new Promise((resolve) => {
    setTimeout(() => {
      const feedback =
        aiFeedbackVariants[Math.floor(Math.random() * aiFeedbackVariants.length)];
      resolve(feedback);
    }, 2500);
  });
}

export async function deepAnalysis(homeworkId: string): Promise<never> {
  Alert.alert(
    'Премиум-функция',
    'Глубокий анализ доступен только в Премиум-подписке. Оформите подписку, чтобы получить развёрнутый разбор вашей работы с рекомендациями.',
    [{ text: 'Понятно' }]
  );
  throw new Error('Требуется Премиум-подписка для глубокого анализа');
}
